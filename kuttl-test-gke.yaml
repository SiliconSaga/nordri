# nordri/kuttl-test-gke.yaml
# Run: kubectl kuttl test --config kuttl-test-gke.yaml
# Covers: ArgoCD apps, Gateway, Crossplane providers/functions, Velero (GKE)
apiVersion: kuttl.dev/v1beta1
kind: TestSuite
testDirs:
  - tests/e2e/shared
  - tests/e2e/gke
timeout: 120
parallel: 1
skipDelete: false
